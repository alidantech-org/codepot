{% set normalized_schema = schema_contract.by_id[enum.api.id] %}
// ============================================================
// AUTO-GENERATED FILE — DO NOT EDIT MANUALLY
// Generated by the CodepotG Dart client fixture pack
// ============================================================

// ── JSON serialisation support for @JsonValue annotations ───
import 'package:json_annotation/json_annotation.dart';

// ============================================================
// {{ enum.name.pascal.o }}
// Normalized schema: {{ normalized_schema.id }} (API Enum)
//
// Represents a closed set of string values defined in the API
// schema. Each Dart variant maps to a specific wire string via
// @JsonValue, ensuring the serialiser sends the exact token the
// API expects rather than the Dart enum name.
//
// Usage:
//   Deserialise — handled automatically by json_annotation
//                 when this enum appears in a model/DTO field.
//   Serialise   — .toJson() on the parent model/DTO will emit
//                 the @JsonValue string, not the Dart name.
// ============================================================
enum {{ enum.name.pascal.o }} {

  // ── Variants ──────────────────────────────────────────────
  // Format: @JsonValue('wire_string') dartVariantName
  //   • wire_string  — the exact token sent over the wire
  //   • dartVariantName — safe Dart identifier (may differ from
  //     the wire value if the original contains hyphens, spaces,
  //     reserved words, or other characters invalid in Dart)
{%- for value in enum.meta.enum_values %}

  // Wire value: '{{ value.wire }}'
  @JsonValue('{{ value.wire }}')
  {{ value.safe_name }}{%- if not loop.last %},{%- endif %}
{%- endfor %}

} // end {{ enum.name.pascal.o }}