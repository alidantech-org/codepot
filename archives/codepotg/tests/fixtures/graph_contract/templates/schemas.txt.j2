node={{ file.meta.node }}
selection={{ selection.name }}
scope={{ selection.scope }}
count={{ selection.count }}
source_count={{ sources | length }}
schemas={% for schema in all_schemas %}{{ schema.name.pascal.o }}{% if not loop.last %},{% endif %}{% endfor %}
document_extension={{ document_contract.extensions["x-graph-marker"] }}
