# Documentation publishing

Canonical authored documentation lives throughout `.docs`. Public website content is selected from `.docs/public` by its `navigation.json` and `ecosystem.json` manifests.

Agent instructions, internal tasks, audits, research drafts, and private planning are not published unless explicitly added to the public navigation manifest.
