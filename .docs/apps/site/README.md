# Codepot site

`apps/site` is the active public website. It currently also validates, compiles, and renders the public Markdown selected from `.docs/public`.

- [`deployment.md`](deployment.md) — current deployment procedure.
- [`../../tasks/apps/site`](../../tasks/apps/site/README.md) — site task area.

The long-term separation between the marketing site and the dedicated documentation application must be implemented through an approved task, while keeping `.docs` as the only authored source.
