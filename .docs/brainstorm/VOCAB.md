Understood. **Everything previously proposed about selection vocabulary, keywords, query syntax, template variables, providers, imports, exports, and barrels is now treated as unapproved.**

You will make the decisions. I will only:

* propose options;
* explain trade-offs;
* test understandability with examples;
* identify risks and contradictions;
* record exactly what you approve, reject, or defer;
* avoid silently extending an approved decision.

## Decision statuses

We will use only these statuses:

```text
UNREVIEWED
PROPOSED
UNDER REVIEW
APPROVED
REJECTED
DEFERRED
```

I will never mark something `APPROVED` unless you explicitly approve it.

## Review order

### 1. Core selection sentence

Decide how a pack author expresses:

```text
What is selected?
How is it filtered?
How many template invocations are created?
```

Status: **UNREVIEWED**

### 2. Selection target vocabulary

Possible concepts to evaluate individually:

```text
package
module
group
property
schema
field
operation
policy
event
storage mapping
value source
view
presentation
workflow
workflow step
```

Questions:

* Which deserve direct selection?
* Which should only appear as variables under another selection?
* Should names be singular or plural?
* Should target names exactly match Runtime IR terms?

Status: **UNREVIEWED**

### 3. Invocation/cardinality vocabulary

We must choose understandable words for:

* exactly one selected item;
* one invocation per item;
* one invocation receiving all items;
* one invocation per grouping.

No previous terms such as `one`, `each`, `all`, `collect`, `groupBy`, `grouped`, or others are approved.

Status: **UNREVIEWED**

### 4. Filter and query design

We must decide:

* whether filters use nested YAML, concise expressions, or another structure;
* supported logical operators;
* how users filter fields, schemas, operations, facets, tags, capabilities, and relationships;
* how much traversal is safe;
* what must remain deliberately unsupported.

Status: **UNREVIEWED**

### 5. Grouping and ordering

We must decide:

* grouping vocabulary;
* allowed grouping values;
* default ordering;
* explicit ordering syntax;
* whether custom grouping is allowed;
* how deterministic ordering is guaranteed.

Status: **UNREVIEWED**

### 6. Template variable vocabulary

For every approved selection, we must document:

* primary selected variable;
* collection variable;
* owner variables;
* related semantic variables;
* universal template variables;
* optional deeper context.

Example questions:

* Does selecting schemas expose `schema` or `item`?
* Are `package`, `module`, and `group` always available?
* Should a collection be called `schemas`, `items`, or `selection`?
* How does a template discover available variables?

Status: **UNREVIEWED**

### 7. Relationship access

We must decide how templates access related meaning:

```text
schema → fields
operation → input/output schemas
schema → operations using it
workflow → steps
view → operations
```

Questions:

* Which relationships are direct variables?
* Which require explicit context requests?
* Which may be used in selection filters?
* How do we avoid arbitrary graph traversal?

Status: **UNREVIEWED**

### 8. Template-to-template dependencies

We must separately define vocabulary for:

* including another template or fragment;
* importing a symbol generated by another template;
* depending on another generated artifact;
* requiring another artifact to exist first.

Status: **UNREVIEWED**

### 9. Providers and generated symbols

We must decide:

* how a template declares what it generates;
* how generated symbols are identified;
* how several representations of one schema are distinguished;
* how another template requests the correct symbol;
* how ambiguity is handled.

No previous vocabulary such as `provides`, `role`, `subject`, or `variant` is approved.

Status: **UNREVIEWED**

### 10. Imports

We must decide how users express:

```text
This generated template needs a symbol produced by that template.
```

The design must explain:

* semantic identity;
* source template;
* imported symbol;
* relative, aliased, package, or barrel paths;
* multiple imported symbols;
* cross-language errors;
* project-provided static imports.

Status: **UNREVIEWED**

### 11. Exports and barrels

We must decide how a barrel template says:

```text
Export emissions created by these templates.
```

Questions:

* Does a barrel reference templates, artifacts, symbols, or selections?
* How does it restrict exports to the current module or group?
* How are names and paths exposed to the barrel template?
* Can it export project bindings and external symbols?

Status: **UNREVIEWED**

### 12. Emission vocabulary

We must choose clear meanings for terms such as:

```text
template
template invocation
emission
artifact
output
generated file
provided symbol
```

Each term must have one meaning across packs, diagnostics, plans, and CLI output.

Status: **UNREVIEWED**

### 13. Conditions and empty selections

We must decide:

* whether templates run when nothing is selected;
* whether empty collections render or skip;
* how exactly-one selections behave with zero or several matches;
* how conditional templates are expressed.

Status: **UNREVIEWED**

### 14. Explainability vocabulary

Every decision must support understandable explanations:

```text
Why was this selected?
Why was it skipped?
What variables were supplied?
What did this template generate?
Why was this import created?
Which template provided this symbol?
```

Status: **UNREVIEWED**

### 15. Scalability constraints

Selection design must remain compatible with:

* large packages;
* module-based loading;
* bounded memory;
* concurrent planning and rendering;
* deterministic ordering;
* planning before output mutation.

These constraints may reject otherwise convenient syntax, but they do not decide user-facing vocabulary by themselves.

Status: **UNREVIEWED**

## Evaluation format for every decision

For each item, I will present:

```text
Problem
User mental model
Option A
Option B
Option C, when useful
Concrete examples
Advantages
Risks
Edge cases
Effect on template variables
Effect on explainability
Effect on scalability
My recommendation
Human decision
```

## Approval record format

I will maintain a record like:

```text
Decision: Selection target naming
Status: APPROVED
Approved rule: ...
Approved examples: ...
Rejected alternatives: ...
Reason: ...
Dependencies: ...
Open questions: ...
```

## Current approval checklist

```text
[ ] Core selection statement
[ ] Selection target vocabulary
[ ] Invocation/cardinality vocabulary
[ ] Filtering/query syntax
[ ] Logical operators
[ ] Relationship filtering
[ ] Grouping
[ ] Ordering
[ ] Template variable names
[ ] Universal context variables
[ ] Optional context
[ ] Template includes/fragments
[ ] Artifact dependencies
[ ] Generated symbol providers
[ ] Generated-code imports
[ ] Exports and barrels
[ ] Static project bindings
[ ] Emission/artifact terminology
[ ] Conditions
[ ] Empty-selection behavior
[ ] Diagnostics and explainability
[ ] Scalability behavior
```

The safest first decision is **the core selection statement shape**, because target names, modes, filters, and variables will all depend on it.


Yes. That is the right starting point.

I would refine it slightly to:

```text
Concept
→ Authorability
→ Selectability
→ Templatability
→ Emitability
→ Composability
→ Auditability
```

The added **Composability** stage covers symbols, imports, exports, barrels, and artifact dependencies.

## Approved foundation

* **Language adapters are eliminated.**
* Dryv resolves semantic items, artifacts, symbols, paths, and dependencies.
* Template packs own language syntax, imports, exports, types, and formatting.

## Review meaning

### 1. Concept

Does this concept belong in the canonical software model?

Examples:

```text
package
module
group
schema
field
operation
event
policy
workflow
```

We determine:

* what it means;
* what owns it;
* what it may contain;
* what it may reference;
* whether it is neutral enough for Dryv.

### 2. Authorability

How can a user define or obtain the concept?

A concept may be:

* directly authored;
* derived from another concept;
* inferred from explicit relationships;
* runtime-created only.

We must avoid concepts that exist only for template convenience but have no real software meaning.

### 3. Selectability

Can a template directly select this concept?

Separate questions:

* Can it be a selection root?
* Can it be filtered?
* Can it be grouped?
* Can related concepts be queried through it?
* What is its deterministic ordering?

Not every concept must be directly selectable.

For example, an operation output may be accessible through an operation without becoming its own selection root.

### 4. Templatability

What does a template receive when this concept is selected?

We decide:

* primary variable name;
* collection variable name;
* owner variables;
* direct child values;
* accessible relationships;
* optional additional context.

Selectability and templatability must remain separate:

> A concept may be available inside template context without being directly selectable.

### 5. Emitability

Can a template invocation produce something for this concept?

Possible emissions include:

* file;
* folder;
* symbol;
* configuration entry;
* documentation section;
* reusable artifact.

We decide:

* what the artifact represents;
* which concept caused it;
* which symbols it defines;
* whether it may emit multiple artifacts;
* whether several templates may emit different representations of the same concept.

### 6. Composability

How does the emitted result interact with other generated results?

This covers:

* symbol definitions;
* symbol consumption;
* direct imports;
* barrel imports;
* exports;
* re-exports;
* path relativity;
* artifact dependencies;
* pack-to-pack connections.

Dryv resolves the graph. Templates render the syntax.

### 7. Auditability

Can users explain the complete journey?

```text
Authored concept
→ Runtime IR concept
→ selection result
→ template context
→ emitted artifact
→ symbol/export route
→ consuming artifacts
```

Users must be able to ask:

* Why was this selected?
* Why was it skipped?
* What variables were available?
* What did it emit?
* Where was the symbol defined?
* Was it imported directly or through a barrel?
* Which authored concept caused this file?

## Human review sheet

For every concept, we can use this checklist:

```text
Concept:
Meaning:
Owner:
Directly authored:
Derived or inferred:
Directly selectable:
Available as related context:
Allowed filters:
Allowed grouping:
Primary template variable:
May emit artifacts:
May define symbols:
May consume symbols:
May be re-exported:
Required trace information:
Status:
```

## Recommended first concept order

Start with the structural foundation:

```text
1. package
2. module
3. group
4. schema
5. field
6. operation
```

Do not design selection keywords yet.

First approve what each concept means and whether it deserves:

```text
authorability
selectability
templatability
emitability
```

After those six concepts are clear, we can derive selection vocabulary instead of inventing keywords before knowing what they must represent.

## Checklist update

```text
[APPROVED] Eliminate language adapters
[APPROVED] Templates own target-language syntax
[APPROVED] Dryv resolves artifacts, symbols, paths, and dependencies

[PROPOSED] Concept review pipeline
[PROPOSED] Add Composability before Auditability
[UNREVIEWED] Concept catalog
[UNREVIEWED] Selection vocabulary
[UNREVIEWED] Template variable vocabulary
```

The first item to review should be **Package as a concept**, followed by Module and Group.
