# Existing task records

Do not create new tasks in this top-level tree. New tasks belong under the owning package or app and are pointed to by `.docs/TODO.md` only while active.

Dryv task records were retired to `.archives/.docs/tasks/dryv` on 2026-08-04 because Dryv is being replanned with no active implementation task.

Existing application and cross-cutting records remain here until their owning documentation folders are migrated. Task templates remain available for reference.
