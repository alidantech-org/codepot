# Dryv CLI

`dryv-cli` is a thin terminal frontend over the public Dryv runtime API. It owns argument parsing, prompts, presentation, JSON output, and exit-code behavior. It does not implement semantic meaning, generation planning, pack behavior, or filesystem ownership rules.

CLI work is tracked under [`../../../tasks/dryv`](../../../tasks/dryv/README.md).
