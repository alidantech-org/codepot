# Task tracking

Use `[ ]` for pending and `[x]` for complete. Mark an active unchecked item with `**IN PROGRESS**`. Every completed task must include focused tests, documentation, and a progress-log row with the landing commit SHA.
