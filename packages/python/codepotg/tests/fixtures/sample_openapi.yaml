openapi: 3.1.0
info:
  title: Riderescue API
  description: OpenAPI fixture for CodepotG inference and generation tests.
  version: v1
servers:
  - url: https://api.riderescueautolink.com
    description: Riderescue API
paths:
  /users:
    x-codegen:
      resource:
        name: users
        path:
          - platform
          - auth
    get:
      operationId: listUsers
      responses:
        "200":
          description: User list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserModel"
    post:
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserBody"
      responses:
        "201":
          description: Created user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserModel"
components:
  schemas:
    BasePublicModel:
      type: object
      x-codegen:
        kind: model
        resource:
          name: shared
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
    UserStatus:
      type: string
      enum:
        - active
        - suspended
      x-codegen:
        kind: enum
        resource:
          name: users
          path:
            - platform
            - auth
    UserModel:
      type: object
      x-codegen:
        kind: model
        resource:
          name: users
          path:
            - platform
            - auth
      required:
        - id
        - name
        - status
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"
    CreateUserBody:
      type: object
      x-codegen:
        kind: dto
        resource:
          name: users
          path:
            - platform
            - auth
      required:
        - name
      properties:
        name:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"
