language={{ lang.name }}
framework={{ lang.framework.name }}
package={{ lang.package.name }}
complete_contract={{ lang.meta.complete_variable_contract }}
project={{ project.name.pascal.o }}
project_format={{ project.emit.format }}
api_title={{ api.info.title }}
openapi_version={{ api.info.openapi_version }}
api_version={{ api.info.api_version }}
server_count={{ api.servers | length }}
server_url={{ api.servers[0].url }}
resource_count={{ resources | length }}
schema_count={{ schemas.all | length }}
model_count={{ schemas.models | length }}
dto_count={{ schemas.dtos | length }}
enum_count={{ schemas.enums | length }}
primitive_count={{ schemas.primitives | length }}
operation_count={{ operations | length }}
entity_count={{ entities | length }}
frontend_count={{ frontends | length }}
selected_frontend_count={{ selected_frontends | length }}
raw_openapi={{ api.raw["openapi"] }}
raw_title={{ api.raw["info"]["title"] }}
lossless_extension={{ api.extensions["x-portable-probe"]["label"] }}
contract_schema_count={{ meta.schema_count }}
contract_operation_count={{ meta.operation_count }}
contract_resource_count={{ meta.resource_count }}
