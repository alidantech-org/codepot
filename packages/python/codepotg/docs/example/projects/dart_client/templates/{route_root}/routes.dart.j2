// ============================================================
// AUTO-GENERATED FILE — DO NOT EDIT MANUALLY
// Generated by the CodepotG Dart client fixture pack
// ============================================================

// ── Per-resource endpoint registries ─────────────────────────
// One import per API resource — each file exposes a const class
// that maps operation names to their resolved URL strings.
{% for resource in features %}
import 'constants/{{ resource.name.path.o | string }}_endpoints.dart';{{ "\n" }}
{% endfor %}

// ============================================================
// {{ project.meta.api_version_name.pascal.o }}Routes
//
// The single source of truth for every API endpoint in this
// version of the API. Holds one typed Endpoints instance per
// resource, all initialised as const fields.
//
// Usage:
// const routes = {{ project.meta.api_version_name.pascal.o }}Routes();
// final path = routes.someResource.someOperation;
// final path = routes.someResource.someOperation(id: '42');
//
// The class is const so it can be instantiated inline at the
// call site without any heap allocation — identical to using
// a singleton but without the global state.
// ============================================================
class {{ project.meta.api_version_name.pascal.o }}Routes {

// All instances are identical — const enforces that guarantee.
const {{ project.meta.api_version_name.pascal.o }}Routes();

// ── Resource Registries ───────────────────────────────────
// Each field is a const Endpoints instance for one resource.
// Access an endpoint via: routes.<resource>.<operation>
    // ─────────────────────────────────────────────────────────
    {% for resource in features %}
    // Endpoints for the "{{ resource.name.pascal.o }}" resource
    final {{ resource.name.camel.o | string }} = const {{ resource.name.pascal.o | string }}Endpoints();{{ "\n" }}
    {% endfor %}

    } // end {{ project.meta.api_version_name.pascal.o }}Routes